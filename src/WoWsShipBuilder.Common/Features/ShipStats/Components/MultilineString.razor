@using System.Web
@using System.Text.RegularExpressions;

@((MarkupString)Regex.Replace(HttpUtility.HtmlEncode(InputString), "\r?\n|\r", "<br />"))

@code {
    [Parameter, EditorRequired]
    public string InputString { get; set; } = default!;
}